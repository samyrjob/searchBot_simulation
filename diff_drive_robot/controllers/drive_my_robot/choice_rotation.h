#ifndef CHOICE_ROTATION_H

#define CHOICE_ROTATION_H



// no pointers, because it's read-only, no intent to modify the values of the distances
const char* rotate(double distance_left, double distance_right);



#endif